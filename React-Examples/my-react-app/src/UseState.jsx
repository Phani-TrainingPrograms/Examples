//React hooks: Sp function that allows function components to use react features without writing class components which was available in older version of React(16.x)
//useState, useEffect, useContext, useReducer useCallback and more. 

//useState = A react hook that allows the creation of a stateful variable and a setter function to update its valie in the V dom. 
import React from 'react'
import MyComponent from './MyComponent'
export default function UseStateEx() {
  return (
    <div>
        <MyComponent/>
    </div>
  )
}
